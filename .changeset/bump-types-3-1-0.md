---
"@klappay/node": patch
---

Bump `@klappay/types` to 3.1.0. Purely additive on the types side (new `CHAIN_IDS` constant and a `@klappay/types/constants` entry point) — no changes to any schema or type this SDK uses, so no code changes here.
